import universidad.*
import materias.*
import carreras.*

describe "universidad" {
	
	// Carrera
	var tpi = new Carrera ( materias = [] )
	var sociologia = new Carrera ( materias = [] )
	
	
	// Materia	                             
    var orga = new Materia(materiasCorrelativas = [],carrera = tpi, curso = [], creditos = 10, anio = 1 )
    
               	                       	                   	                       	                   
	// Estudiante	
	var nacho = new Estudiante( carreras =[tpi,sociologia],materiasAprobadas =[orga], cantidadDeCreditos = 150 )
	
	
  
test "testX" {
	assert.that(nacho.estaIncripto(orga))
}

     
}